
import type { Client } from '../types';

/**
 * Dados iniciais do sistema. 
 * Alterado para array vazio para que o usuário inicie apenas com seus próprios lançamentos.
 */
export const initialClientsData: Client[] = [];
